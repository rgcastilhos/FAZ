# Copy to android/keystore.properties (do not commit) and fill with your values.
# storeFile path is relative to the android/ folder (this Gradle rootProject).
storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME

