services:
  - type: web
    name: react-example
    env: docker
    autoDeploy: true
    healthCheckPath: /api/health
    disk:
      name: app-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /var/data
      - key: ADMIN_CODE
        value: "153720"
      - key: ADMIN_USERNAME
        value: "RKI"
